import React from 'react'
import { View, Text } from 'react-native'

// const typescriptdemo = (a: number,b: number) => a+b

// console.log(typescriptdemo('a', 'b'))

// export default typescriptdemo
